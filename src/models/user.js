export default class User {
  constructor(id, email, password, name, restarantName) {
    this.id = id;
    this.email = email;
    this.password = password;
    this.restarantName = restarantName;
    this.name = name;
  }
}
